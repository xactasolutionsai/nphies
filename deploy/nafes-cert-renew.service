[Unit]
Description=Renew NAFES short-lived IP TLS certificate
After=network-online.target
[Service]
Type=oneshot
ExecStart=/opt/certbot/bin/certbot renew --quiet --deploy-hook "systemctl reload nginx"
